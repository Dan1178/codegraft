# Normalize line endings: store text as LF in the repo, regardless of OS.
# Keeps diffs clean and avoids CRLF churn between Windows and CI/Linux.
* text=auto eol=lf

# Binary assets git should never touch.
*.png binary
*.jpg binary
*.gif binary
*.ico binary
